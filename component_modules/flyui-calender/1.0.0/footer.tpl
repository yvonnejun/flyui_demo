<div class="calender-foot clearfix">
    {{if ok}}
    <button type="button" class="ok btn btn-primary btn-sm">{{ok}}</button>
    {{/if}}
    {{if clear}}
    <button type="button" class="cancel btn btn-defalut btn-sm">{{clear}}</button>
    {{/if}}
    <span class="now">{{now}}</span>
</div>